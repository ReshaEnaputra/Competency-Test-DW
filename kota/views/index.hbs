{{>header}}
{{>topnav}}
    <div class="mainContainer">
        <select name="select" onchange="location = this.value">
            <option value="/">Provinsi</option>
            <option value="/kabupaten">Kabupaten</option>
        </select>
        {{#each provinsi}}
        <div class="cardContainer">
                <div class="card">
                    <div class="imgContainer">
                        <img class="img" src="{{this.image}}">
                    </div>
                    <p class="p">{{this.nama}}</p>
                    <p class="p">{{this.tgl}}</p>
                    <div class="aContainer">
                        <a href="/provinsi/detail/{{this.id}}" class="detail">Detail</a>
                        <a href="/provinsi/edit/{{this.id}}" class="detail">Edit</a>
                        <a href="/provinsi/delete/{{this.id}}" class="detail">Delete</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
{{>footer}}